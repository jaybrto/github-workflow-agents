---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gwa-runner
  namespace: default
  labels:
    app: github-workflow-agents
    component: runner
spec:
  serviceName: gwa-runner
  replicas: 1
  selector:
    matchLabels:
      app: github-workflow-agents
      component: runner
  template:
    metadata:
      labels:
        app: github-workflow-agents
        component: runner
    spec:
      terminationGracePeriodSeconds: 30
      imagePullSecrets:
        - name: ghcr-secret
      securityContext:
        fsGroup: 1000
      initContainers:
        - name: fix-permissions
          image: busybox
          command:
            - sh
            - -c
            - |
              chown -R 1000:1000 /home/runner/.claude
              chown -R 1000:1000 /home/runner/worktrees
              chown -R 1000:1000 /home/runner/repo
          volumeMounts:
            - name: claude-session
              mountPath: /home/runner/.claude
            - name: worktrees
              mountPath: /home/runner/worktrees
            - name: repo
              mountPath: /home/runner/repo
      containers:
        - name: gwa-agent
          image: ghcr.io/jaybrto/github-workflow-agents:latest
          imagePullPolicy: Always
          command: ["/bin/bash", "/scripts/entrypoint.sh"]
          resources:
            requests:
              memory: "2Gi"
              cpu: "1000m"
            limits:
              memory: "4Gi"
              cpu: "2000m"
          env:
            - name: REPO
              value: "jaybrto/github-workflow-agents"
            - name: REDIS_HOST
              value: "redis-haproxy.redis-system.svc.cluster.local"
            - name: REDIS_PORT
              value: "6379"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          volumeMounts:
            - name: claude-session
              mountPath: /home/runner/.claude
            - name: worktrees
              mountPath: /home/runner/worktrees
            - name: repo
              mountPath: /home/runner/repo
            - name: init-script
              mountPath: /scripts/entrypoint.sh
              subPath: entrypoint.sh
              readOnly: true
      volumes:
        - name: init-script
          configMap:
            name: gwa-runner-init
            defaultMode: 0755
  volumeClaimTemplates:
    - metadata:
        name: claude-session
        labels:
          app: github-workflow-agents
          component: session-storage
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: longhorn-claude
        resources:
          requests:
            storage: 10Gi
    - metadata:
        name: worktrees
        labels:
          app: github-workflow-agents
          component: worktree-storage
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: longhorn-claude
        resources:
          requests:
            storage: 30Gi
    - metadata:
        name: repo
        labels:
          app: github-workflow-agents
          component: repo-storage
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: longhorn-claude
        resources:
          requests:
            storage: 10Gi
